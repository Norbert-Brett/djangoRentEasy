# Nixpacks configuration for Railway deployment
# Only override the build phase to add collectstatic

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
    "mkdir -p logs"
]

[start]
cmd = "python manage.py migrate && gunicorn djangorenteasy.wsgi --log-file -"
